# plugin-context.md — Estado atual do plugin

> Gerenciado por `glpi-plugin-context`. Nunca editar manualmente.
> Atualizar apenas após inspeção real do código — o conteúdo deve refletir o estado atual do código, não suposições.
> Atualizar apenas as seções afetadas pela inspeção mais recente.

---

## Metadados da última inspeção

- Inspecionado em: —
- Arquivos inspecionados: —
- Versão do plugin: —

---

## Identificação do plugin

- Chave do plugin (`plugin_key`): —
- Nome exibido: —
- Versão mínima do GLPI: —
- Versão mínima do PHP: —

---

## Tabelas do banco de dados

> Preencher após inspeção de `install/` e das classes de migration.

| Tabela | Propósito |
|--------|-----------|
| — | — |

---

## Direitos registrados

> Preencher após inspeção de `setup.php` e das classes que definem direitos.

| Constante | O que controla |
|-----------|---------------|
| — | — |

---

## Controllers e rotas

> Preencher após inspeção de `src/` e dos atributos `#[Route(...)]`.

| Rota | Controller | Método | Requer autenticação |
|------|-----------|--------|-------------------|
| — | — | — | — |

---

## Hooks registrados

> Preencher após inspeção de `setup.php` e `hook.php`.

| Hook | Handler | Quando é acionado |
|------|---------|------------------|
| — | — | — |

---

## Abas (Tabs) registradas

> Preencher após inspeção das classes que implementam `getTabNameForItem` e `displayTabContentForItem`.

| Aba | Classe | Item GLPI ao qual se anexa |
|-----|--------|--------------------------|
| — | — | — |

---

## CronTasks registradas

> Preencher após inspeção de `setup.php` e das classes que implementam o handler do cron.

| Task | Frequência | Handler |
|------|-----------|---------|
| — | — | — |

---

## Notificações registradas

> Preencher após inspeção de `setup.php` e das classes de notificação.

| Evento | Quando dispara | Destinatários padrão |
|--------|---------------|---------------------|
| — | — | — |

---

## Assets registrados

> Preencher após inspeção de `setup.php` e dos helpers de carregamento de assets.

| Arquivo | Tipo | Carregado em |
|---------|------|-------------|
| — | JS/CSS | — |
